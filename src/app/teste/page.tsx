"use client";

import { DisplayInfo } from "@/components/DisplayInfo/DisplayInfo";
import EditPunchCard from "@/components/EditPunchCard/EditPunchCard";
import PunchModal from "@/components/PunchModal/PunchModal";
import useRealtimePunch from "@/hooks/useRealtimePunch";
import {
  Box,
  Button,
  Card,
  CardActionArea,
  CardContent,
  TextField,
  Typography,
  ToggleButtonGroup,
  ToggleButton

} from "@mui/material";

export default function Teste() {
  return (
    <Box
      sx={{ display: "flex", justifyContent: "center", alignItems: "center" }}
    >
    </Box>
  );
}
